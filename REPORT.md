# Лабораторная работа №1
## Базовый последовательный алгоритм умножения матриц (без распараллеливания)

---

## 1. Цель работы
Реализовать программу последовательного умножения квадратных матриц в общей памяти на одном ядре процессора и оценить влияние компиляторных оптимизаций `-O2` и `-O3` на время выполнения.

---

## 2. Теоретическая часть

### 2.1. Постановка задачи
Пусть заданы две квадратные матрицы `A` и `B` размера `N × N`. Нужно вычислить матрицу `C`:

\[
C = A \cdot B, \quad c_{ij} = \sum_{k=1}^{N} a_{ik} b_{kj}
\]

### 2.2. Асимптотика
- По времени: **O(N³)**
- По памяти (без учёта входа):
  - обычный алгоритм: **O(N²)** на матрицу `C`
  - кэш-оптимизированный (с транспонированием `B`): дополнительно **O(N²)** на `B^T`

### 2.3. Почему кэш-оптимизация ускоряет вычисления
В обычной схеме элементы `B[k][j]` читаются по столбцу, что хуже для кэша CPU. После транспонирования `B` получаем `B^T[j][k]`, и внутренний цикл читает данные последовательно в памяти. Это повышает cache locality и уменьшает cache misses.

---

## 3. Реализация
Файл программы: `matrix_benchmark.cpp`.

Реализованы 2 алгоритма:
1. `naive` — классическое умножение `i-j-k`.
2. `cache` — транспонирование `B` и умножение с последовательным доступом к памяти.

Дополнительно реализовано:
- прогрев (warm-up) перед замером,
- несколько повторов на каждый размер,
- вывод `best`, `avg`, `stddev`,
- `checksum` для контроля корректности.

---

## 4. Оборудование и ПО
- **CPU:** Intel Core i5-12400F
- **RAM:** 32 GB DDR4
- **OS:** Windows 11
- **IDE:** VS Code
- **Компилятор:** GCC (указать версию командой `g++ --version`)

---

## 5. Инструкция по запуску

### 5.1. Сборка
```bash
g++ -std=c++17 -O2 -march=native -o matmul_O2 matrix_benchmark.cpp
g++ -std=c++17 -O3 -march=native -o matmul_O3 matrix_benchmark.cpp
```

### 5.2. Формат запуска
```bash
./matmul_O3 <algo> <runs> <N1> <N2> ...
```
Где:
- `<algo>`: `naive` или `cache`
- `<runs>`: число повторов для каждого размера (рекомендуется 3–7)
- `<N...>`: размеры матриц

Пример:
```bash
./matmul_O2 cache 5 256 512 768 1024
./matmul_O3 cache 5 256 512 768 1024
```

---

## 6. Методика измерений
1. Закрыть лишние приложения.
2. Для каждого размера `N` выполнить замер минимум 3 раза (`runs >= 3`).
3. Использовать одинаковые размеры и одинаковые команды для `-O2` и `-O3`.
4. В таблицу заносить среднее `avg_sec`; дополнительно хранить `best_sec`.
5. Проверять совпадение `checksum` между `-O2` и `-O3`.

Формула ускорения:
\[
S = \frac{T_{O2}}{T_{O3}}
\]

---

## 7. Результаты эксперимента (шаблон)

### 7.1. Алгоритм `cache`

| N | best O2, c | avg O2, c | best O3, c | avg O3, c | Ускорение `avg(O2)/avg(O3)` |
|---:|---:|---:|---:|---:|---:|
| 256  | _..._ | _..._ | _..._ | _..._ | _..._ |
| 512  | _..._ | _..._ | _..._ | _..._ | _..._ |
| 768  | _..._ | _..._ | _..._ | _..._ | _..._ |
| 1024 | _..._ | _..._ | _..._ | _..._ | _..._ |

### 7.2. Алгоритм `naive` (контрольное сравнение)

| N | avg O2, c | avg O3, c | Ускорение |
|---:|---:|---:|---:|
| 256 | _..._ | _..._ | _..._ |
| 512 | _..._ | _..._ | _..._ |

---

## 8. Анализ результатов (что написать в отчёте)
1. Подтвердить зависимость времени от `N` близкую к кубической.
2. Показать преимущество `-O3` над `-O2`.
3. Показать преимущество `cache` над `naive` для средних/больших `N`.
4. Отметить, что использовалось **одно ядро** и не применялись многопоточные библиотеки.

---

## 9. Места для скриншотов

### Скриншот 1 — компиляция
![Сборка O2/O3](./screenshots/build_placeholder.png)

### Скриншот 2 — запуск `cache` с `-O2`
![Запуск O2](./screenshots/run_o2_placeholder.png)

### Скриншот 3 — запуск `cache` с `-O3`
![Запуск O3](./screenshots/run_o3_placeholder.png)

### Скриншот 4 — таблица результатов (из Word/Excel)
![Таблица результатов](./screenshots/table_placeholder.png)

---

## 10. Вывод (готовый шаблон)
В ходе лабораторной работы реализовано последовательное умножение матриц двумя способами: классическим (`naive`) и кэш-оптимизированным (`cache`). Эксперименты показали, что при росте размера матриц время вычислений растёт примерно как `O(N^3)`. Применение флага `-O3` в среднем снижает время выполнения относительно `-O2`, а кэш-оптимизированная версия демонстрирует дополнительное ускорение за счёт более эффективного доступа к памяти.
