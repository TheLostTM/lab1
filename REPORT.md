# Лабораторная работа №1
## Тема: Оптимизация без распараллеливания

**Цель:** реализовать последовательное умножение двух квадратных матриц в общей памяти (одно ядро) и сравнить время выполнения при разных флагах оптимизации компилятора.

## 1) Что реализовано
Файл программы: `matrix_benchmark.cpp`.

В программе есть 2 варианта умножения:
1. `naive` — классический алгоритм «строка на столбец» (`i-j-k`).
2. `cache` — более кэш-дружелюбный вариант: сначала транспонируется матрица `B`, затем перемножение идёт по непрерывным участкам памяти.

Для проверки корректности после умножения выводится `checksum` (сумма всех элементов результирующей матрицы).

---

## 2) Как собрать и запустить (Windows 11 + GCC)

### Вариант через терминал VS Code
Открой терминал в папке проекта и выполни:

```bash
g++ -std=c++17 -O2 -march=native -o matmul_O2 matrix_benchmark.cpp
g++ -std=c++17 -O3 -march=native -o matmul_O3 matrix_benchmark.cpp
```

Запуск (кэш-оптимизированный):
```bash
./matmul_O2 cache 256 512 768 1024
./matmul_O3 cache 256 512 768 1024
```

Запуск (обычный алгоритм):
```bash
./matmul_O2 naive 256 512
./matmul_O3 naive 256 512
```

> Если используешь MinGW в Windows, бинарники могут называться `matmul_O2.exe` и `matmul_O3.exe`.

### Вариант через `tasks.json` в VS Code (по желанию)
Можно создать 2 задачи сборки: одна с `-O2`, вторая с `-O3`.
Ключевое отличие — только флаг оптимизации.

---

## 3) Методика измерений
1. Фиксировать один и тот же набор размеров матриц (например: `256, 512, 768, 1024`).
2. Для каждого флага (`-O2`, `-O3`) запускать одинаковую команду.
3. Записать `time_sec` из вывода программы.
4. Сравнить результаты.
5. (Рекомендуется) Повторить каждый тест 3–5 раз и взять среднее.

Пример команды:
```bash
./matmul_O3 cache 256 512 768 1024
```

---

## 4) Таблица результатов (заполнить своими замерами)

**CPU:** Intel Core i5-12400F  
**RAM:** 32 GB DDR4  
**OS:** Windows 11  
**Компилятор:** GCC (указать версию: `g++ --version`)

### 4.1 Кэш-оптимизированный алгоритм (`cache`)

| Размер N | Время `-O2`, сек | Время `-O3`, сек | Ускорение `O2/O3` |
|---:|---:|---:|---:|
| 256  | _заполнить_ | _заполнить_ | _заполнить_ |
| 512  | _заполнить_ | _заполнить_ | _заполнить_ |
| 768  | _заполнить_ | _заполнить_ | _заполнить_ |
| 1024 | _заполнить_ | _заполнить_ | _заполнить_ |

### 4.2 Обычный алгоритм (`naive`) — для сравнения

| Размер N | Время `-O2`, сек | Время `-O3`, сек |
|---:|---:|---:|
| 256 | _заполнить_ | _заполнить_ |
| 512 | _заполнить_ | _заполнить_ |

---

## 5) Места для скриншотов

### Скриншот 1 — сборка с `-O2` и `-O3`
> Вставь сюда скрин терминала VS Code с командами компиляции.

![Скриншот сборки](./screenshots/build_placeholder.png)

### Скриншот 2 — запуск и время для `-O2`
> Вставь сюда скрин с выводом `./matmul_O2 cache ...`

![Скриншот запуска O2](./screenshots/run_o2_placeholder.png)

### Скриншот 3 — запуск и время для `-O3`
> Вставь сюда скрин с выводом `./matmul_O3 cache ...`

![Скриншот запуска O3](./screenshots/run_o3_placeholder.png)

---

## 6) Вывод (шаблон)
1. При увеличении размера матрицы время растёт примерно как `O(N^3)`.
2. Флаг `-O3` обычно быстрее `-O2` (точный прирост зависит от CPU и компилятора).
3. Кэш-оптимизированный вариант (`cache`) обычно заметно быстрее обычного (`naive`) за счёт лучшей локальности данных.
4. Для одноядерного последовательного решения это базовый уровень оптимизации без распараллеливания.

---

## 7) Пример быстрых команд для отчёта

```bash
g++ --version
./matmul_O2 cache 256 512 768 1024
./matmul_O3 cache 256 512 768 1024
```
